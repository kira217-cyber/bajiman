// Static site identity for the Guide app. No API calls — edit these values
// directly to change branding.

import footerLogo from "../assets/footer/footer-logo.png";

export const siteConfig = {
  siteName: { en: "Bajiman", bn: "বাজিমান" },
  logo: footerLogo,
};

// Hero banner slides. Empty by default (no static banner images supplied
// yet) — add entries like { desktopImageUrl, mobileImageUrl } to populate
// the homepage slider.
export const sliders = [];
